name: Build & Distribute

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    uses: ./.github/jobs/build.yml

  deploy:
    name: Deploy
    needs: build
    uses: ./.github/jobs/deploy.yml
