name: Build & Distribute

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml

  deploy:
    name: Deploy
    needs: build
    uses: ./.github/jobs/deploy.yml
